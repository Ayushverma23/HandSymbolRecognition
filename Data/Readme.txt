At first we have to collect dataset the first step is collect dataset by running collect image.py that will create the dataset 
While running create dataset it will create and load dataset from tensorflow and convert our collected image into model.pickle
